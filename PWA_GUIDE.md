# あじfly! - PWA対応ガイド\n\n## 📱 PWA（Progressive Web App）対応について\n\nあじfly!がPWA対応になりました。以下の機能が利用可能です。\n\n### ✨ PWAの主な機能\n\n#### 1. **ホーム画面にインストール**\n- スマートフォン・タブレットのホーム画面にアプリのようにインストール可能\n- ネイティブアプリのように起動できます\n\n**iOS（Safari）:**\n1. ブラウザのメニューボタン（上部右側）をタップ\n2. 「ホーム画面に追加」をタップ\n3. 「追加」をタップ\n\n**Android（Chrome）:**\n1. ブラウザのメニューボタン（右上・3点）をタップ\n2. 「インストール」をタップ\n3. または、ブラウザが自動で「インストール」ボタンを表示する場合があります\n\n#### 2. **オフラインプレイ対応**\n- Service Workerにより、インターネット接続なしでプレイ可能\n- 初回アクセス時に必要なファイルを自動キャッシュ\n- インターネット接続が復帰すると自動で同期\n\n#### 3. **高速読み込み**\n- キャッシュされたファイルから高速に起動\n- 2回目以降の起動はほぼ瞬時\n\n---\n\n## 💾 データ保存と引継ぎ\n\n### 保存システムの改善\n\nPWA対応により、データ保存は以下の方式に対応しました：\n\n**旧方式（従来）:**\n- `localStorage` のみ使用\n- 容量制限あり（通常5-10MB）\n- ブラウザのキャッシュ削除で消失する可能性\n\n**新方式（推奨）:**\n- `IndexedDB` で保存（容量が大きい）\n- より安全で永続的\n- `localStorage` との自動同期\n\n### 📤 データ引継ぎ方法\n\n#### ステップ1: 引継ぎコードを生成\n\n1. ゲーム内の「ステータス」画面を開く\n2. 新しく追加された「📤 データ引継ぎ」ボタンをタップ\n3. QRコード＆テキストコードが表示される\n\n**3つの選択肢:**\n- **QRコード**: 別のデバイスでカメラアプリで読み込める\n- **テキストコード**: 「コピー」ボタンで長いコードをコピー\n- **手動**: コードを手動でメモして別デバイスで入力\n\n#### ステップ2: 別のデバイスで復元\n\n1. 別のデバイス（スマートフォン・PC等）であじfly!を開く\n2. 「ステータス」→「📥 データ復元」ボタンをタップ\n3. 前のステップで取得したコードを入力\n4. 「OK」をタップで復元完了\n\n---\n\n## 🔒 セキュリティについて\n\n### チェックサム検証\n- すべての引継ぎコードにチェックサムが含まれます\n- 改ざんされたコードは検証時に自動で検出・破棄\n- 不正なコードの入力に対しても安全\n\n### データ暗号化\n- 引継ぎコードはBase64エンコード（簡易暗号化）\n- QRコードはサーバーサイドで生成（サーバーにデータは保存されない）\n\n---\n\n## 🆘 トラブルシューティング\n\n### Q: インストールボタンが表示されない\n**A:** \n- Androidの場合：Chrome最新版を使用してください\n- iOSの場合：Safari 11.3以上が必要です\n- 一度ページをリロードしてみてください\n\n### Q: オフラインでプレイできない\n**A:**\n- Service Workerがキャッシュするまで待ってください（初回アクセス後数分）\n- ブラウザ設定で「オフラインファイル」を有効にしてください\n- デベロッパーツール（F12）の「Application」→「Cache Storage」でキャッシュを確認できます\n\n### Q: データ復元時に「無効な引継ぎコード」エラー\n**A:**\n- コードをすべてコピーできているか確認\n- 手動入力の場合は、大文字・小文字を区別します\n- 改行やスペースが含まれていないか確認\n\n### Q: IndexedDBのデータが壊れた\n**A:**\n- ブラウザのキャッシュをクリアしてください\n- デベロッパーツール → \"Application\" → \"IndexedDB\" → \"AziflyDB\" を削除\n- ページをリロードすると自動で再初期化されます\n\n### Q: 古いデータが残っている\n**A:**\n- localStorageから自動的にIndexedDBに移行されます\n- 両方に同じデータが保存されるため、どちらかが正常ならデータは保持されます\n\n---\n\n## ⚙️ 詳細技術情報\n\n### ファイル構成\n\n```\n├── manifest.json          # PWAマニフェストファイル\n├── sw.js                 # Service Worker\n├── storage.js            # IndexedDB管理\n├── datatransfer.js       # データ引継ぎロジック\n├── ui-datashare.js       # UIコンポーネント\n├── save.js               # 保存システム（IndexedDB対応版）\n└── index.html            # PWAメタタグ追加版\n```\n\n### Service Workerの動作\n\n1. **インストール時**: 必要なファイルをキャッシュ\n2. **実行時**: キャッシュ優先で取得（ネットワークフォールバック）\n3. **アップデート時**: 古いキャッシュを自動削除\n\n### IndexedDBの構造\n\n```\nデータベース名: AziflyDB\nバージョン: 1\n\nオブジェクトストア: gamedata\n  キー: 'savedata'\n  値: {\n    bestScore, coins, totalCoins, rubies,\n    eqSkin, skins, achieves, challenges,\n    worldClassDone, level, xp, levelCap,\n    levelCapUnlockCount\n  }\n```\n\n---\n\n## 🔄 マイグレーション情報\n\n### localStorageからIndexedDBへ\n\n初回起動時に以下の処理が自動実行されます：\n\n1. IndexedDBが存在するか確認\n2. なければlocalStorageから読み込み\n3. IndexedDBに保存\n4. 以降、双方に保存（互換性維持）\n\n### バージョンアップ時\n\n- 新しいキャッシュが自動ダウンロード\n- 古いキャッシュは自動削除\n- ユーザーデータは保持\n\n---\n\n## 📊 ストレージ容量\n\n| ストレージ種別 | 容量制限 | 用途 |\n|---|---|---|\n| localStorage | 5-10MB | 従来互換性用 |\n| IndexedDB | 50-1GB | メインデータ保存 |\n| Service Worker Cache | 50-1GB | ゲームファイルキャッシュ |\n\n**合計**: ブラウザ仕様に依存（通常100MB-1GB）\n\n---\n\n## 🚀 推奨環境\n\n### デスクトップ\n- Chrome 40+\n- Firefox 44+\n- Safari 11.1+（macOS 10.13+）\n- Edge 15+\n\n### モバイル\n- iOS 11.3+ (Safari)\n- Android 5.0+ (Chrome)\n\n---\n\n## 📝 注記\n\n- PWA機能はブラウザのサポート状況に依存します\n- オフラインモードはキャッシュの内容に限定されます\n- デバイス間のデータ同期は引継ぎコード経由でのみ可能です\n- 定期的にバックアップすることをお勧めします\n\n---\n\n**最終更新**: 2026年8月4日\n"